import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.example.maze',
  appName: 'Maze Puzzler',
  webDir: 'dist',
  bundledWebRuntime: false,
}

export default config
